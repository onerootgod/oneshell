# 🤖 AGENTS.md

这份文件是给所有后续 agent 的统一作战说明。只要你进入这个仓库，就默认要遵守这里的方向，不要自己发明另一套路线。

## 🎯 你在做什么

你正在参与重建 **OneShell**。

目标：

- 做一个超过 FinalShell 的 macOS 终端与远程工作台
- 基于 `Tauri + Rust + React`
- 强化终端渲染、Emoji、SFTP、脚本联动和本地高级功能

## 🧱 唯一允许的技术主线

- 🖥️ 桌面壳：`Tauri 2`
- 🦀 后端：`Rust`
- ⚛️ 前端：`React 18`
- 🎨 样式：`Tailwind CSS`
- 🧵 终端：`xterm.js`
- 🔐 存储：`SQLite + SQLCipher`
- 🛡️ 敏感字段：`AES-256-GCM`

## ❌ 禁止事项

以下内容一律禁止重新引回主线：

- 旧的 `SwiftUI` 主界面
- 旧的 `core/` 运行时目录
- 旧的 `ui/` 原生工程
- 旧的 `.github/` 工作流链路
- 任何“为了兼容旧架构而回退”的代码
- 把仓库重新做成双主线并存

## ✅ 文档规则

所有新增或重写文档，默认都必须满足：

- 使用 `Emoji + 中文`
- 先写目标，再写现状，再写下一步
- 不写模糊口号，要写清楚当前已完成和未完成
- 不要把“计划中”写成“已完成”

如果你改了架构或推进了阶段，至少同步更新这些文件：

- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`

## 🗺️ 默认推进顺序

如果用户没有单独改优先级，按下面顺序推进：

1. 🖥️ `xterm.js` 终端主渲染链路
2. 🔌 Rust SSH 连接管理器
3. 🌍 SOCKS5 代理支持
4. 📡 Tauri 事件桥：stdin / stdout / lifecycle
5. 🧰 脚本工作站
6. 📂 SFTP 文件管理
7. 👑 本地 license / 高级功能门禁

## 🧪 每次推进的最小交付要求

每次做完一轮工作，至少要交代：

- 这次具体推进了什么
- 改了哪些关键文件
- 做了哪些验证
- 当前最大阻塞是什么

## 🧩 当前仓库状态

当前仓库已经完成：

- 新仓库重建
- Tauri 基础配置
- React + Tailwind 基础骨架
- SQLCipher 基础加密存储
- AES-GCM 密码字段加密
- xterm.js + Unicode11 + WebGL fallback 基础终端组件

当前最大工程阻塞：

- Rust SSH runtime 还没真正打通
- Tauri 前后端事件桥还没完整连上
- Linux 远端缺少 `glib-2.0`，不能拿它做完整 Tauri 编译验证

## 📌 决策口径

出现选择时，优先遵守这些判断：

- 为了长期可维护性，宁可删旧，不要兼容污染
- 为了 macOS 体验，终端渲染正确性优先于表面功能数
- 为了后续 agent 接力，文档清晰度和架构一致性优先
- 为了安全，本地加密与密钥边界优先
