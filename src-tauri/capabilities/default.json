{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the OneShell rebuild.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
